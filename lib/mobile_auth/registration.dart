import 'package:blockexample/mobile_auth/screen/sign_in.dart';
import 'package:flutter/material.dart';

class SignInMobileOtp extends StatelessWidget {
  const SignInMobileOtp({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Colors.grey,
      body: SignMobile(),
    );
  }
}
