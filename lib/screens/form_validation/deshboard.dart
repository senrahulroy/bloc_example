import 'package:flutter/material.dart';

class DeshBoad extends StatelessWidget {
  const DeshBoad({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("You are in "),
    );
  }
}
